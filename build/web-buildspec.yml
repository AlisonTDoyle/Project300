version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm i
      - npm install -g @angular/cli
  build:
    commands:
      - ng build
artifacts:
  files:
      - '**/*'
  base-directory: 'dist/timetable-application/browser*'
  discard-paths: yes